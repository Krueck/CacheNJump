{
  "name": "cachenjump",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.2.1"
  }
}